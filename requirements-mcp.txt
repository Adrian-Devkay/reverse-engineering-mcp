mcp>=1.12,<2
lief>=0.17.0
capstone>=5.0,<6
yara-python>=4.5,<5

# For the current Linux x86_64 / CPython 3.13 profile, install from
# requirements-mcp.lock.txt with --require-hashes after reviewing its source.
